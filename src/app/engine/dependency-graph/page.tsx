import { EngineStub } from "@/components/engine/engine-stub";

export default function DependencyGraphPage() {
  return <EngineStub title="Dependency Graph" badge="L3 Structural Graph" description="Visualise multi-tier supplier relationships, node criticality scores, and kill-chain path analysis across the active simulation topology." />;
}
